<%= simple_form_for(@movie) do |form| %>
  <%= form.input :five_words, placeholder: 'Urban Horse Learns About Sex' %>
  <%= form.input :title, placeholder: 'Sex and the City' %>
  <%= form.button :submit %>
<% end %>